pipeline {
    agent any

    stages{

        stage('Checkout){
            steps{
                git(
                    url: 'https://github.com/nkrao8506/learn.git',
                    branch: 'main'
                )
            }
        }
    }
}
